name: PR build

on: [push, pull_request]

jobs:
  my_job1_ubuntu-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew build --scan --no-daemon
      - name: Archive Error Log
        uses: actions/upload-artifact@v2
        if: failure()
        with:
            name: Ballerina Internal Log
            path: tcp-ballerina/ballerina-internal.log
            if-no-files-found: ignore
      - name: Archive Code Coverage JSON
        uses: actions/upload-artifact@v2
        with:
            name: Code Coverage JSON
            path: tcp-ballerina/target/report/test_results.json
            if-no-files-found: ignore
  
  my_job2_ubuntu-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew build --scan --no-daemon
      - name: Archive Error Log
        uses: actions/upload-artifact@v2
        if: failure()
        with:
            name: Ballerina Internal Log
            path: tcp-ballerina/ballerina-internal.log
            if-no-files-found: ignore
      - name: Archive Code Coverage JSON
        uses: actions/upload-artifact@v2
        with:
            name: Code Coverage JSON
            path: tcp-ballerina/target/report/test_results.json
            if-no-files-found: ignore
  
  my_job3_ubuntu-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew build --scan --no-daemon
      - name: Archive Error Log
        uses: actions/upload-artifact@v2
        if: failure()
        with:
            name: Ballerina Internal Log
            path: tcp-ballerina/ballerina-internal.log
            if-no-files-found: ignore
      - name: Archive Code Coverage JSON
        uses: actions/upload-artifact@v2
        with:
            name: Code Coverage JSON
            path: tcp-ballerina/target/report/test_results.json
            if-no-files-found: ignore
  
  my_job4_ubuntu-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew build --scan --no-daemon
      - name: Archive Error Log
        uses: actions/upload-artifact@v2
        if: failure()
        with:
            name: Ballerina Internal Log
            path: tcp-ballerina/ballerina-internal.log
            if-no-files-found: ignore
      - name: Archive Code Coverage JSON
        uses: actions/upload-artifact@v2
        with:
            name: Code Coverage JSON
            path: tcp-ballerina/target/report/test_results.json
            if-no-files-found: ignore
  
  my_job5_ubuntu-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew build --scan --no-daemon
      - name: Archive Error Log
        uses: actions/upload-artifact@v2
        if: failure()
        with:
            name: Ballerina Internal Log
            path: tcp-ballerina/ballerina-internal.log
            if-no-files-found: ignore
      - name: Archive Code Coverage JSON
        uses: actions/upload-artifact@v2
        with:
            name: Code Coverage JSON
            path: tcp-ballerina/target/report/test_results.json
            if-no-files-found: ignore
  
  my_job6_ubuntu-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew build --scan --no-daemon
      - name: Archive Error Log
        uses: actions/upload-artifact@v2
        if: failure()
        with:
            name: Ballerina Internal Log
            path: tcp-ballerina/ballerina-internal.log
            if-no-files-found: ignore
      - name: Archive Code Coverage JSON
        uses: actions/upload-artifact@v2
        with:
            name: Code Coverage JSON
            path: tcp-ballerina/target/report/test_results.json
            if-no-files-found: ignore
  
  my_job7_ubuntu-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew build --scan --no-daemon
      - name: Archive Error Log
        uses: actions/upload-artifact@v2
        if: failure()
        with:
            name: Ballerina Internal Log
            path: tcp-ballerina/ballerina-internal.log
            if-no-files-found: ignore
      - name: Archive Code Coverage JSON
        uses: actions/upload-artifact@v2
        with:
            name: Code Coverage JSON
            path: tcp-ballerina/target/report/test_results.json
            if-no-files-found: ignore
  
  my_job8_ubuntu-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew build --scan --no-daemon
      - name: Archive Error Log
        uses: actions/upload-artifact@v2
        if: failure()
        with:
            name: Ballerina Internal Log
            path: tcp-ballerina/ballerina-internal.log
            if-no-files-found: ignore
      - name: Archive Code Coverage JSON
        uses: actions/upload-artifact@v2
        with:
            name: Code Coverage JSON
            path: tcp-ballerina/target/report/test_results.json
            if-no-files-found: ignore
  
  my_job9_ubuntu-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew build --scan --no-daemon
      - name: Archive Error Log
        uses: actions/upload-artifact@v2
        if: failure()
        with:
            name: Ballerina Internal Log
            path: tcp-ballerina/ballerina-internal.log
            if-no-files-found: ignore
      - name: Archive Code Coverage JSON
        uses: actions/upload-artifact@v2
        with:
            name: Code Coverage JSON
            path: tcp-ballerina/target/report/test_results.json
            if-no-files-found: ignore
  
  my_job10_ubuntu-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew build --scan --no-daemon
      - name: Archive Error Log
        uses: actions/upload-artifact@v2
        if: failure()
        with:
            name: Ballerina Internal Log
            path: tcp-ballerina/ballerina-internal.log
            if-no-files-found: ignore
      - name: Archive Code Coverage JSON
        uses: actions/upload-artifact@v2
        with:
            name: Code Coverage JSON
            path: tcp-ballerina/target/report/test_results.json
            if-no-files-found: ignore
  
  my_job11_ubuntu-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew build --scan --no-daemon
      - name: Archive Error Log
        uses: actions/upload-artifact@v2
        if: failure()
        with:
            name: Ballerina Internal Log
            path: tcp-ballerina/ballerina-internal.log
            if-no-files-found: ignore
      - name: Archive Code Coverage JSON
        uses: actions/upload-artifact@v2
        with:
            name: Code Coverage JSON
            path: tcp-ballerina/target/report/test_results.json
            if-no-files-found: ignore
  
  my_job12_ubuntu-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew build --scan --no-daemon
      - name: Archive Error Log
        uses: actions/upload-artifact@v2
        if: failure()
        with:
            name: Ballerina Internal Log
            path: tcp-ballerina/ballerina-internal.log
            if-no-files-found: ignore
      - name: Archive Code Coverage JSON
        uses: actions/upload-artifact@v2
        with:
            name: Code Coverage JSON
            path: tcp-ballerina/target/report/test_results.json
            if-no-files-found: ignore
  
  my_job13_ubuntu-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew build --scan --no-daemon
      - name: Archive Error Log
        uses: actions/upload-artifact@v2
        if: failure()
        with:
            name: Ballerina Internal Log
            path: tcp-ballerina/ballerina-internal.log
            if-no-files-found: ignore
      - name: Archive Code Coverage JSON
        uses: actions/upload-artifact@v2
        with:
            name: Code Coverage JSON
            path: tcp-ballerina/target/report/test_results.json
            if-no-files-found: ignore
  
  my_job14_ubuntu-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew build --scan --no-daemon
      - name: Archive Error Log
        uses: actions/upload-artifact@v2
        if: failure()
        with:
            name: Ballerina Internal Log
            path: tcp-ballerina/ballerina-internal.log
            if-no-files-found: ignore
      - name: Archive Code Coverage JSON
        uses: actions/upload-artifact@v2
        with:
            name: Code Coverage JSON
            path: tcp-ballerina/target/report/test_results.json
            if-no-files-found: ignore
  
  my_job15_ubuntu-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew build --scan --no-daemon
      - name: Archive Error Log
        uses: actions/upload-artifact@v2
        if: failure()
        with:
            name: Ballerina Internal Log
            path: tcp-ballerina/ballerina-internal.log
            if-no-files-found: ignore
      - name: Archive Code Coverage JSON
        uses: actions/upload-artifact@v2
        with:
            name: Code Coverage JSON
            path: tcp-ballerina/target/report/test_results.json
            if-no-files-found: ignore
  
  my_job16_ubuntu-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew build --scan --no-daemon
      - name: Archive Error Log
        uses: actions/upload-artifact@v2
        if: failure()
        with:
            name: Ballerina Internal Log
            path: tcp-ballerina/ballerina-internal.log
            if-no-files-found: ignore
      - name: Archive Code Coverage JSON
        uses: actions/upload-artifact@v2
        with:
            name: Code Coverage JSON
            path: tcp-ballerina/target/report/test_results.json
            if-no-files-found: ignore
  
  my_job17_ubuntu-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew build --scan --no-daemon
      - name: Archive Error Log
        uses: actions/upload-artifact@v2
        if: failure()
        with:
            name: Ballerina Internal Log
            path: tcp-ballerina/ballerina-internal.log
            if-no-files-found: ignore
      - name: Archive Code Coverage JSON
        uses: actions/upload-artifact@v2
        with:
            name: Code Coverage JSON
            path: tcp-ballerina/target/report/test_results.json
            if-no-files-found: ignore
  
  my_job18_ubuntu-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew build --scan --no-daemon
      - name: Archive Error Log
        uses: actions/upload-artifact@v2
        if: failure()
        with:
            name: Ballerina Internal Log
            path: tcp-ballerina/ballerina-internal.log
            if-no-files-found: ignore
      - name: Archive Code Coverage JSON
        uses: actions/upload-artifact@v2
        with:
            name: Code Coverage JSON
            path: tcp-ballerina/target/report/test_results.json
            if-no-files-found: ignore
  
  my_job19_ubuntu-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew build --scan --no-daemon
      - name: Archive Error Log
        uses: actions/upload-artifact@v2
        if: failure()
        with:
            name: Ballerina Internal Log
            path: tcp-ballerina/ballerina-internal.log
            if-no-files-found: ignore
      - name: Archive Code Coverage JSON
        uses: actions/upload-artifact@v2
        with:
            name: Code Coverage JSON
            path: tcp-ballerina/target/report/test_results.json
            if-no-files-found: ignore
  
  my_job20_ubuntu-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew build --scan --no-daemon
      - name: Archive Error Log
        uses: actions/upload-artifact@v2
        if: failure()
        with:
            name: Ballerina Internal Log
            path: tcp-ballerina/ballerina-internal.log
            if-no-files-found: ignore
      - name: Archive Code Coverage JSON
        uses: actions/upload-artifact@v2
        with:
            name: Code Coverage JSON
            path: tcp-ballerina/target/report/test_results.json
            if-no-files-found: ignore
  
  my_job21_ubuntu-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew build --scan --no-daemon
      - name: Archive Error Log
        uses: actions/upload-artifact@v2
        if: failure()
        with:
            name: Ballerina Internal Log
            path: tcp-ballerina/ballerina-internal.log
            if-no-files-found: ignore
      - name: Archive Code Coverage JSON
        uses: actions/upload-artifact@v2
        with:
            name: Code Coverage JSON
            path: tcp-ballerina/target/report/test_results.json
            if-no-files-found: ignore
  
  my_job22_ubuntu-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew build --scan --no-daemon
      - name: Archive Error Log
        uses: actions/upload-artifact@v2
        if: failure()
        with:
            name: Ballerina Internal Log
            path: tcp-ballerina/ballerina-internal.log
            if-no-files-found: ignore
      - name: Archive Code Coverage JSON
        uses: actions/upload-artifact@v2
        with:
            name: Code Coverage JSON
            path: tcp-ballerina/target/report/test_results.json
            if-no-files-found: ignore
  
  my_job23_ubuntu-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew build --scan --no-daemon
      - name: Archive Error Log
        uses: actions/upload-artifact@v2
        if: failure()
        with:
            name: Ballerina Internal Log
            path: tcp-ballerina/ballerina-internal.log
            if-no-files-found: ignore
      - name: Archive Code Coverage JSON
        uses: actions/upload-artifact@v2
        with:
            name: Code Coverage JSON
            path: tcp-ballerina/target/report/test_results.json
            if-no-files-found: ignore
  
  my_job24_ubuntu-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew build --scan --no-daemon
      - name: Archive Error Log
        uses: actions/upload-artifact@v2
        if: failure()
        with:
            name: Ballerina Internal Log
            path: tcp-ballerina/ballerina-internal.log
            if-no-files-found: ignore
      - name: Archive Code Coverage JSON
        uses: actions/upload-artifact@v2
        with:
            name: Code Coverage JSON
            path: tcp-ballerina/target/report/test_results.json
            if-no-files-found: ignore
  
  my_job25_ubuntu-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew build --scan --no-daemon
      - name: Archive Error Log
        uses: actions/upload-artifact@v2
        if: failure()
        with:
            name: Ballerina Internal Log
            path: tcp-ballerina/ballerina-internal.log
            if-no-files-found: ignore
      - name: Archive Code Coverage JSON
        uses: actions/upload-artifact@v2
        with:
            name: Code Coverage JSON
            path: tcp-ballerina/target/report/test_results.json
            if-no-files-found: ignore
  
  my_job26_ubuntu-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew build --scan --no-daemon
      - name: Archive Error Log
        uses: actions/upload-artifact@v2
        if: failure()
        with:
            name: Ballerina Internal Log
            path: tcp-ballerina/ballerina-internal.log
            if-no-files-found: ignore
      - name: Archive Code Coverage JSON
        uses: actions/upload-artifact@v2
        with:
            name: Code Coverage JSON
            path: tcp-ballerina/target/report/test_results.json
            if-no-files-found: ignore
  
  my_job27_ubuntu-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew build --scan --no-daemon
      - name: Archive Error Log
        uses: actions/upload-artifact@v2
        if: failure()
        with:
            name: Ballerina Internal Log
            path: tcp-ballerina/ballerina-internal.log
            if-no-files-found: ignore
      - name: Archive Code Coverage JSON
        uses: actions/upload-artifact@v2
        with:
            name: Code Coverage JSON
            path: tcp-ballerina/target/report/test_results.json
            if-no-files-found: ignore
  
  my_job28_ubuntu-build:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v1
        - name: Set up JDK 11
          uses: joschi/setup-jdk@v2
          with:
            java-version: 11
        - name: Build with Gradle
          env:
            packageUser: ${{ github.actor }}
            packagePAT: ${{ secrets.GITHUB_TOKEN }}
          run: ./gradlew build --scan --no-daemon
        - name: Archive Error Log
          uses: actions/upload-artifact@v2
          if: failure()
          with:
              name: Ballerina Internal Log
              path: tcp-ballerina/ballerina-internal.log
              if-no-files-found: ignore
        - name: Archive Code Coverage JSON
          uses: actions/upload-artifact@v2
          with:
              name: Code Coverage JSON
              path: tcp-ballerina/target/report/test_results.json
              if-no-files-found: ignore

  my_job1_windows-build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew.bat build --scan --no-daemon
    
  my_job2_windows-build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew.bat build --scan --no-daemon
    
  my_job3_windows-build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew.bat build --scan --no-daemon
    
  my_job4_windows-build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew.bat build --scan --no-daemon
    
  my_job5_windows-build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew.bat build --scan --no-daemon
    
  my_job6_windows-build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew.bat build --scan --no-daemon
    
  my_job7_windows-build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew.bat build --scan --no-daemon
    
  my_job8_windows-build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew.bat build --scan --no-daemon
    
  my_job9_windows-build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew.bat build --scan --no-daemon
    
  my_job10_windows-build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew.bat build --scan --no-daemon
    
  my_job11_windows-build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew.bat build --scan --no-daemon
    
  my_job12_windows-build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew.bat build --scan --no-daemon
    
  my_job13_windows-build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew.bat build --scan --no-daemon
    
  my_job14_windows-build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew.bat build --scan --no-daemon
    
  my_job15_windows-build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew.bat build --scan --no-daemon
    
  my_job16_windows-build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew.bat build --scan --no-daemon
    
  my_job17_windows-build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew.bat build --scan --no-daemon
    
  my_job18_windows-build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew.bat build --scan --no-daemon
    
  my_job19_windows-build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew.bat build --scan --no-daemon
    
  my_job20_windows-build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew.bat build --scan --no-daemon
    
  my_job21_windows-build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew.bat build --scan --no-daemon
    
  my_job22_windows-build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew.bat build --scan --no-daemon
    
  my_job23_windows-build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew.bat build --scan --no-daemon
    
  my_job24_windows-build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew.bat build --scan --no-daemon
    
  my_job25_windows-build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew.bat build --scan --no-daemon
    
  my_job26_windows-build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew.bat build --scan --no-daemon
    
  my_job27_windows-build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew.bat build --scan --no-daemon
    
  my_job28_windows-build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: joschi/setup-jdk@v2
        with:
          java-version: 11
      - name: Build with Gradle
        env:
          packageUser: ${{ github.actor }}
          packagePAT: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew.bat build --scan --no-daemon
   
    